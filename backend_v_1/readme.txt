need to download glove.6B
need to install pytorch
need cuda and cudnn and gpu

firstly, cd to the "backend" directory

secondly, run command "python raw2dict.py"

finally, run command "python train.py --shuffle_train"

Training part is finished, but the inference part is on going.
